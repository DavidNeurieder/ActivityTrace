New features:
- Statistics screen with summary cards, timeline charts, top apps, breakdown
- Blocked system apps auto-configured on fresh install
- Diagnostic error logs for export and backup failures

Improvements:
- Timeline charts now same size — no jumping when switching tabs
- Top apps always visible (even with 0 or 1 entry)
- Breakdown always shown after timeline
- More detailed export error messages

Fixes:
- Export no longer crashes on some Android 16 devices
- SQLite backup no longer fails on retry (stale temp file)
- No more duplicate breakdowns when filtering by app in statistics
- Overlapping numbers in daily chart bars fixed

Multi-language support added in previous update.