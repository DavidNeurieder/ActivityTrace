# Activity Trace

On-device cross-app memory search for Android. No AI, no cloud, no models.

Search across notifications, screenshots, clipboard, and files with natural language queries — all encrypted at rest with SQLCipher + Android Keystore.

## Build

```bash
./gradlew assembleDebug
```

## License

GPL-3.0-only
